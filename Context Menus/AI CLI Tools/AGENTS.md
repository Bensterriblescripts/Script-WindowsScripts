Windows 11.
Be concise. Guess if ambiguous; don’t ask.
Ignore all version control data; do not use git history, diffs, blame, branches, tags, PRs, patches, or derived metadata, and do not use git to reconstruct prior states.
Prefer longer functions over small abstractions.
Inline small repeated logic and small optimizations instead of extracting helpers or one-line wrappers.
Avoid frameworks unless already present.
Avoid comments unless requested.
